{
    "cols": [
      {% for col in columns %}
        {"id": "{{col.id}}", "label": "{{col.label}}", "type": "{{col.type}}"}
        {% if forloop.last  %}
          ],
        {% else %}
          ,
        {% endif %}
      {% endfor %}
    "rows": [
  {% for record in records %}
    {"c":[{"v": "{{record.key}}"}, {"f": "{{record.value}}"}]}
    {% if forloop.last %}
      ]
     {% else %}
      ,
    {% endif %}
  {% endfor %}
}
