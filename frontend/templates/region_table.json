{
    "cols": [{'id': 'region', 'label': 'Регион', 'type': 'string'},
             {'id': 'sum', 'label': 'Сумма', 'type': 'number'}],
    "rows": [
  {% for record in records %}
    {"c":[{"v": "<a href='/region?code={{record.key}}'>{{record.key}}</a>"}, {"f": "{{record.value}}"}]}
  {% endfor %}
    ]
}
